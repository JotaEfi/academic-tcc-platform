// Deprecated static Ziggy config.
// Kept empty because the app now uses the dynamic
// Ziggy object injected by the @routes directive in
// `resources/views/app.blade.php`.

export const Ziggy = {
    url: '',
    port: null,
    defaults: {},
    routes: {},
};
